<Window x:Class="Magazin.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Magazin.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid Margin="290,75,377,209">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="UsernameTextBox" Margin="10" Text="user" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="10" Password="user"/>
        <Button Content="Login" Grid.Row="2" Margin="10" Click="LoginButton_Click"/>
    </Grid>
</Window>
